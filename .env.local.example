# ============================================================
# PRONTOJOB — Variables de Entorno
# Copia este archivo como .env.local y rellena los valores
# ============================================================

# URL base de la app (sin barra final)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------
# STRIPE — Configura esto en https://dashboard.stripe.com
# -----------------------------------------------

# Clave secreta de Stripe (empieza por sk_live_ o sk_test_)
STRIPE_SECRET_KEY=sk_test_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Clave pública de Stripe (empieza por pk_live_ o pk_test_)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Secret del webhook (empieza por whsec_)
# Obtenerlo en: Dashboard → Developers → Webhooks → tu endpoint → Signing secret
STRIPE_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# IDs de precio de los productos en Stripe
# Crear en: Dashboard → Products → Add product → Add price
# Elegir "Recurring" con facturación mensual
STRIPE_PRICE_PRO=price_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
STRIPE_PRICE_PRO_PLUS=price_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# -----------------------------------------------
# WEBHOOK ENDPOINT
# En producción: https://tupdominio.com/api/stripe/webhook
# En desarrollo: usar Stripe CLI → stripe listen --forward-to localhost:3000/api/stripe/webhook
# -----------------------------------------------
